apiVersion: apps/v1
kind: Deployment
metadata: { name: recommendation-service, namespace: microshop }
spec:
  replicas: 1
  selector: { matchLabels: { app: recommendation-service } }
  template:
    metadata: { labels: { app: recommendation-service } }
    spec:
      containers:
      - name: recommendation-service
        image: amirul135/microshop-recommendation-service:latest
        ports: [{ containerPort: 8080 }]
---
apiVersion: v1
kind: Service
metadata: { name: recommendation-service, namespace: microshop }
spec:
  selector: { app: recommendation-service }
  ports: [{ port: 8080, targetPort: 8080 }]
