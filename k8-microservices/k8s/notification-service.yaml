apiVersion: apps/v1
kind: Deployment
metadata: { name: notification-service, namespace: microshop }
spec:
  replicas: 1
  selector: { matchLabels: { app: notification-service } }
  template:
    metadata: { labels: { app: notification-service } }
    spec:
      containers:
      - name: notification-service
        image: amirul135/microshop-notification-service:latest
        ports: [{ containerPort: 8080 }]
---
apiVersion: v1
kind: Service
metadata: { name: notification-service, namespace: microshop }
spec:
  selector: { app: notification-service }
  ports: [{ port: 8080, targetPort: 8080 }]
