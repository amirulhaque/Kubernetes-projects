apiVersion: apps/v1
kind: Deployment
metadata: { name: shipping-service, namespace: microshop }
spec:
  replicas: 1
  selector: { matchLabels: { app: shipping-service } }
  template:
    metadata: { labels: { app: shipping-service } }
    spec:
      containers:
      - name: shipping-service
        image: amirul135/microshop-shipping-service:latest
        ports: [{ containerPort: 8080 }]
---
apiVersion: v1
kind: Service
metadata: { name: shipping-service, namespace: microshop }
spec:
  selector: { app: shipping-service }
  ports: [{ port: 8080, targetPort: 8080 }]
